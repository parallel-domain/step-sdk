syntax = "proto3";

package pd.data;

import "options.proto";


message GenerateMotionVectors2DConfig
{
    bool enable_child_mesh_rotation = 1;
	MotionVectorMode mode = 2;
	string output_path = 3                [(type)=eImage, (stream)=eOutput];
}
